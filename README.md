
  # Create app functions

  This is a code bundle for Create app functions. The original project is available at https://www.figma.com/design/7JJ3l84ykG1CHsiFbSnBLE/Create-app-functions.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  